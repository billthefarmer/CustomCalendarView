
apply plugin: 'com.android.application'

android {
    compileSdkVersion 'android-22'
    buildToolsVersion '24.0.2'

    lintOptions {
        disable 'OldTargetApi', 'IconDensities'
	// abortOnError false
    }
}

dependencies {
    compile project(':library')
}
